package com.example.hatchtracker.data.mappers

import com.example.hatchtracker.data.models.IncubationEntity
import com.example.hatchtracker.model.Incubation

fun IncubationEntity.toModel(): Incubation {
    return Incubation(
        id = id,
        flockId = flockId,
        birdId = birdId,
        fatherBirdId = fatherBirdId,
        species = species,
        breeds = breeds,
        startDate = startDate,
        expectedHatch = expectedHatch,
        eggsCount = eggsCount,
        hatchedCount = hatchedCount,
        infertileCount = infertileCount,
        failedCount = failedCount,
        actualHatchDate = actualHatchDate,
        hatchNotes = hatchNotes,
        notes = notes,
        hatchCompleted = hatchCompleted,
        sourceScenarioId = sourceScenarioId,
        incubationProfileId = incubationProfileId,
        incubatorDeviceId = incubatorDeviceId,
        hatcherDeviceId = hatcherDeviceId,
        syncId = syncId,
        lifecycleStage = lifecycleStage,
        lastUpdated = lastUpdated,
        ownerUserId = ownerUserId,
        cloudId = cloudId,
        flockCloudId = flockCloudId,
        birdCloudId = birdCloudId,
        fatherBirdCloudId = fatherBirdCloudId,
        serverUpdatedAt = serverUpdatedAt,
        localUpdatedAt = localUpdatedAt,
        deleted = deleted,
        syncState = syncState,
        actionPlanId = actionPlanId,
        generationIndex = generationIndex,
        breedLabelOverride = breedLabelOverride,
        breederPoolBirdIds = breederPoolBirdIds,
        costBasisCents = costBasisCents,
        assetAllocationCents = assetAllocationCents,
        costBasisSchemaVersion = costBasisSchemaVersion,
        isCostFrozen = isCostFrozen
    )
}

fun Incubation.toEntity(): IncubationEntity {
    return IncubationEntity(
        id = id,
        flockId = flockId,
        birdId = birdId,
        fatherBirdId = fatherBirdId,
        species = species,
        breeds = breeds,
        startDate = startDate,
        expectedHatch = expectedHatch,
        eggsCount = eggsCount,
        hatchedCount = hatchedCount,
        infertileCount = infertileCount,
        failedCount = failedCount,
        actualHatchDate = actualHatchDate,
        hatchNotes = hatchNotes,
        notes = notes,
        hatchCompleted = hatchCompleted,
        sourceScenarioId = sourceScenarioId,
        incubationProfileId = incubationProfileId,
        incubatorDeviceId = incubatorDeviceId,
        hatcherDeviceId = hatcherDeviceId,
        syncId = syncId,
        lifecycleStage = lifecycleStage,
        lastUpdated = lastUpdated,
        ownerUserId = ownerUserId,
        cloudId = cloudId,
        flockCloudId = flockCloudId,
        birdCloudId = birdCloudId,
        fatherBirdCloudId = fatherBirdCloudId,
        serverUpdatedAt = serverUpdatedAt,
        localUpdatedAt = localUpdatedAt,
        deleted = deleted,
        syncState = syncState,
        actionPlanId = actionPlanId,
        generationIndex = generationIndex,
        breedLabelOverride = breedLabelOverride,
        breederPoolBirdIds = breederPoolBirdIds,
        costBasisCents = costBasisCents,
        assetAllocationCents = assetAllocationCents,
        costBasisSchemaVersion = costBasisSchemaVersion,
        isCostFrozen = isCostFrozen
    )
}
