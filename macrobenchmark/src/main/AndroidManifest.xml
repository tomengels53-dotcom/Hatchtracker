<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Macrobenchmark uses UiAutomator which requires testOnly="false" if we are sideloading but test handles this.
         Empty manifest. -->
</manifest>
